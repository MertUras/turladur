/** Local fallback used when remote or missing images fail to load. */
export const IMAGE_PLACEHOLDER = '/images/empty-bookings.svg';

/** Default logo path (SVG avoids missing logo.png 404s). */
export const LOGO_PATH = '/images/logo.svg';
