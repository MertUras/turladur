{
  "name": "shared-validators",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/shared-validators/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:validators"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm --filter @turta/shared-validators build",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @turta/shared-validators lint",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
